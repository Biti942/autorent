rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Autorise lecture pour tous, mais écriture seulement aux utilisateurs connectés
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
